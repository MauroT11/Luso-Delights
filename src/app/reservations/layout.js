export const metadata = {
    title: "Luso Delights - Reservations",
    description: "Book a table at Luso Delights and enjoy the authentic flavors of Portugal.",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }