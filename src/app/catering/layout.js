export const metadata = {
    title: "Luso Delights - Catering",
    description: "Let us cater to your needs with our delightful menu options.",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }