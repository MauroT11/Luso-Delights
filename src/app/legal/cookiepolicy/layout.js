export const metadata = {
    title: "Luso Delights - Cookie Policy",
    description: "This is the Cookie Policy for Sabor Delights, accessible from our website.",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }