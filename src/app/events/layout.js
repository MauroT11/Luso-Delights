export const metadata = {
    title: "Luso Delights - Events",
    description: "Join us for our upcoming events and enjoy the authentic flavors of Portugal.",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }