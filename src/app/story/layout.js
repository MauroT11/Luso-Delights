export const metadata = {
    title: "Luso Delights - Our Story",
    description: "Discover the story behind Luso Delights and how we bring the authentic flavors of Portugal to your table.",
  };
  
  export default async function RecipesLayout({ children }) {
    return (
        <main>{children}</main>
    );
  }